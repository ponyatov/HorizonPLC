/*for (let i = 1;i<15;i++) {
    let StrTmp = 'I2C' + i;
    if (eval('${StrTmp} instanceof I2C')) {
        console.log (StrTmp + ' is an object!')
    }
    else {
        console.log (StrTmp + ' is NOT an object!')
    }
}*/

//

for (var i=0;i==0 || Pin(i);i++) print(Pin(i))
/*print (Pin(0));
print (Pin(1));
print (+Pin(0));*/